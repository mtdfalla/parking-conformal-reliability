# Clean-room image for the replication package. Generated by
# 03_code/2026-09-04-build-package.py — do not hand-edit.
# Built to match the environment every
# published number was computed under, recorded in
# 05_results/tables/2026-09-04-environment-recorded.csv.
#
# NOTE, and it matters: this image installs from the LOCK FILE's wheels and pins NO system
# BLAS. The recorded environment loads two different bundled OpenBLAS builds at once
# (numpy's and scipy's, scipy-openblas 0.3.29), and forcing a single system BLAS would be
# a DIFFERENT numerical environment from the one the numbers come from.
#
#   docker build -f 03_code/2026-09-04-Dockerfile -t parking-cp .
#   docker run --rm -v "$PWD":/work -w /work parking-cp bash run_all.sh
FROM python:3.10.12-slim
# recorded host: Linux-6.8.0-136-generic-x86_64-with-glibc2.35 / glibc-2.35 / BLAS scipy-openblas 0.3.29 / LAPACK scipy-openblas 0.3.29
WORKDIR /work
COPY 03_code/2026-09-04-requirements.lock.txt /tmp/lock.txt
RUN pip install --no-cache-dir --require-hashes --no-deps -r /tmp/lock.txt
ENV PYTHONDONTWRITEBYTECODE=1
CMD ["bash", "run_all.sh"]
