# PRESPECIFICATION — revision 48, the clean-room run from the revision-48 package (EXP-039)

**Written 2026-09-06, revision 48, AFTER the package was built and BEFORE it was unzipped or any step run.**
Fourteenth prespecification. Authority: S47 record §11 item 6; the author's go of 2026-09-06. Constrained by
rule 11 (tolerances do not move after a run), C13 (manifest re-cut LAST), C14 (nothing archived is copied into
the output directory), C16 (verify in the tree actually run AND in a fresh unzip carrying only the outputs),
C18 (the gate is exit code + zero failures + the regenerated COUNT), A9, A10/A16.

## 0. The artifact, measured on the shipped zip only

`06_manuscript/CTR/replication/2026-09-06-parking-conformal-reliability.zip`, sha256 `885581665211c17e…`,
**281 files**, 252 manifest entries, 124 reference tables. Its predecessor (the revision-46 build, sha256
`b0c9379f…`, 270 files) was retired byte-intact as `_superseded/2026-09-06-…-S46.zip` and **was never run end
to end**; the last completed clean-room run is EXP-038 on the 2026-09-05 zip.

## 1. What changed since EXP-038, and the gate for each

| change | class | gate |
|---|---|---|
| `2026-09-06-exp037-pooled-vs-worst.py` + runner step `exp037` | new CODE + 2 DATA | both CSVs regenerated **byte-identical** to the shipped reference (96 and 12 rows) |
| `2026-09-05-spatial-{analysis,mantel}.py` write `2026-09-06-spatial-statistics.csv` | CODE + DATA | regenerated byte-identical (8 rows); the two scripts' other outputs within the `tight` class as before |
| `2026-09-06-figures-f1-f2.py` + runner step `figures` (second command) | CODE + 4 figure files | step exits 0, prints "off-scale markers: 1", four files written (not hashed — figures are `not_gated`) |
| `2026-09-04-build-package.py`, `verify.py` text | CODE | exact-hash entries, unchanged by the run |
| `EXPERIMENTS_LOG.md` gains EXP-037 | doc | **unmutated by the run** (defect 7 stays fixed): sha256 equal before and after |

## 2. Predictions

| # | prediction | source |
|---|---|---|
| P1 | `run_all.sh` completes every step, exit 0, in **~55–60 `device_bash` calls** (`timeout 172` each; `--from <step>` to advance) | EXP-036/038 rate |
| P2 | `verify.py` inside the run tree: **zero failures**; regenerated count **65** (EXP-038's 62 + the three new tables); archived note **58**; checks **309** (306 + 3) | C18 |
| P3 | the same in a fresh unzip carrying only `05_results/tables/` and `05_results/figures/` from the run (C16) | C16 |
| P4 | Belgrade delay rows 147 with facility 8 absent; Birmingham 210; e3 row counts 164 / 196 | EXP-038 negative controls, unchanged |
| P5 | No registered unstable cell is drawn, or if one is, `verify.py` names it from `00_admin/2026-09-05-unstable-cells.csv` and it is not counted as a failure | S45 registry |

## 3. Branches, decided now

- **A (all hold):** EXP-039 logged CLOSED; repository manifest re-cut (supersede by date); no package rebuild
  needed unless a package-listed file changed during the run.
- **B (a numeric cell differs outside tolerance):** the tolerance does not move (rule 11); the differing artifact
  is named in the log and the session ends with the package NOT approved.
- **F (a step fails):** classify infrastructure (A10/A16: disk, vanished pylibs — re-run STEP 0, then the step)
  before code; a code defect is fixed at source, the package rebuilt, and the run restarted from that step with
  the restart recorded.
- **S (an artifact is not regenerated and not archived):** a defect of the runner or script (C11/C18 class), never
  added to the archived list to make the count pass.

## 4. What this run does NOT cover

The Dockerfile (docker/podman absent on this machine — still untested); the manuscript build chain (A20).
